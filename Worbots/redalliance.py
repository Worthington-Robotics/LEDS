from machine import Pin
import neopixel
import time

np = neopixel.NeoPixel(Pin(28), 156)

while True:
    for i in range(156): 
        difference = j-i
        if difference > 1:
            np[j]: (255,0, 0)
